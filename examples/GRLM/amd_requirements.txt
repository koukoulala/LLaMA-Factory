# Key packages for LLaMA-Factory SFT on AMD MI300X (ROCm 7.0)
# Generated from actual working environment on 2026-03-30
# Note: torch upgraded to 2.10.0+rocm7.0 via pip (from pytorch rocm7.0 index)
#       vllm/flash_attn are pre-installed from the Docker image
#       causal_conv1d built from source with --no-build-isolation (ROCm HIP)

# === Core Training Framework ===
llamafactory==0.9.5.dev0      # installed via: pip install -e ".[torch,metrics]"
deepspeed==0.16.4
accelerate==1.12.0
peft==0.18.1
trl==0.28.0                   # must be <0.29 for LLaMA-Factory compat

# === Model & Tokenizer ===
transformers==5.4.0           # must be >=5.4 for Qwen3.5 support
huggingface_hub==1.8.0
tokenizers==0.22.2
safetensors==0.7.0
sentencepiece==0.2.1

# === Kernel Acceleration ===
liger-kernel==0.7.0+dev        # installed from GitHub main: pip install git+https://github.com/linkedin/Liger-Kernel.git
                               # PyPI 0.7.0 lacks Qwen3.5 support; GitHub main has apply_liger_kernel_to_qwen3_5
fla-core==0.4.2
flash-linear-attention==0.4.2
causal_conv1d==1.6.1           # built from source: cd /tmp/causal-conv1d && pip install --no-build-isolation .

# === Data Processing ===
datasets==4.5.0
numpy==2.2.6
pandas==3.0.0
scipy==1.17.1

# === Torch stack (installed from https://download.pytorch.org/whl/rocm7.0) ===
torch==2.10.0+rocm7.0          # upgraded from 2.9.1 to avoid Conv3D perf regression
torchvision==0.25.0+rocm7.0
torchaudio==2.10.0+rocm7.0
triton-rocm==3.6.0

# === Pre-installed from Docker image (DO NOT pip install) ===
# flash_attn==2.8.3
# triton_kernels==1.0.0
# vllm==0.16.0rc2+rocm700

# === UI (optional) ===
gradio==6.10.0
matplotlib==3.10.8
